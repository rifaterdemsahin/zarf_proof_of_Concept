# /init-project

Bootstrap all 7 stage folders and root config files for a new project using this template.

## Usage
```
/init-project
/init-project "My Project Name"
```

## What this command does

Sets up a new project from `delivery-pilot-template` by:

1. Confirming the project name (used in `CLAUDE.md`, `README.md`, `agents.md`)
2. Creating the 7 stage folders if they don't exist, each with a `README.md`
3. Copying `.claude/` config to root (settings, commands)
4. Running `/init` to generate a fresh `CLAUDE.md` for the new codebase
5. Creating `index.html` with both Project Menu and Debug Menu wired up
6. Initializing `6_Semblance/error.log` and `6_Semblance/fix.log`
7. Making the first commit: `chore: initialize project from delivery-pilot-template`

## Instructions for the agent

When the user runs `/init-project`:

### Step 1 — Gather inputs (ASK THESE QUESTIONS FIRST)

Before creating or modifying any files, **ask the user the setup questionnaire below** (use the `AskUserQuestion` tool, one batch of questions). Do not assume answers — the whole point of init is to configure the template consciously. Record every answer in `1_Real_Unknown/prompts.md` and in `4_Formula/decisions.md`.

#### 🧱 Project basics
1. **Project name?** (required — used in `CLAUDE.md`, `README.md`, `agents.md`)
2. **GitHub username?** (default: `rifaterdemsahin`)
3. **Repo name?** (used for the GitHub Pages URL `https://<user>.github.io/<repo>/`)

#### ☁️ Tooling choices (pick what this project will actually use)
4. **Backend deployments — use Fly.io?** (container-based Docker deploys) → Yes/No. If No, what hosts the backend, or is it frontend-only?
5. **Database — use Supabase?** (managed Postgres, auth, realtime, storage) → Yes / No / Other (which?).
6. **Server-side logs — use Axiom?** (centralized structured logging) → Yes / No / Other.
7. **Edge compute — use Cloudflare Workers?** (auth/routing/caching) → Yes / No.
8. **AI / vector — Ollama + Qdrant, Supabase `pgvector`, or none?**
9. **Secrets — confirm Azure Key Vault** (per env: dev/staging/prod) → Yes / Other vault.

#### 🚀 Delivery
10. **Environments needed?** (dev / staging / prod — which ones?)
11. **Custom domain for GitHub Pages?** (Yes → which / No)

After collecting answers:
- Prune `2_Environment/` docs for tools the project will **not** use (move to `2_Environment/_obsolete/`), and trim their secrets from `.env.example`.
- Update `2_Environment/tools.md`, `architecture.md` (Mermaid), and the debug-menu config to reflect only the chosen tools.
- Seed Azure Key Vault names per chosen environment.

### Step 2 — Create folder structure
```
1_Real_Unknown/README.md   — The "Why": OKRs and problem definitions
2_Environment/README.md    — The "Context": setup guides and constraints
3_Simulation/README.md     — The "Vision": UI mockups and images
4_Formula/README.md        — The "Recipe": step-by-step guides
5_Symbols/README.md        — The "Reality": source code
6_Semblance/README.md      — The "Scars": errors, workarounds, lessons
7_Testing_Known/README.md  — The "Proof": validation and checklists
```

### Step 3 — Configure .claude at root
- Copy `4_Formula/.claude/settings.template.json` → `.claude/settings.json`
- Copy `4_Formula/.claude/commands/` → `.claude/commands/`

### Step 4 — Initialize tracking files
- Create `6_Semblance/error.log` with header template
- Create `6_Semblance/fix.log` with header template
- Create `6_Semblance/lessons_learned.md` with init entry
- Create `1_Real_Unknown/prompts.md` with header

### Step 5 — First commit
```
git add .
git commit -m "chore: initialize <project-name> from delivery-pilot-template"
git push -u origin main
```

## Checklist after /init-project

- [ ] Setup questionnaire asked and answers recorded in `1_Real_Unknown/prompts.md` + `4_Formula/decisions.md`
- [ ] Unused tool docs pruned to `2_Environment/_obsolete/` and `.env.example` trimmed
- [ ] `2_Environment/tools.md` reflects only the chosen tools
- [ ] All 7 folders exist with README.md
- [ ] `.claude/settings.json` has git permissions pre-approved
- [ ] `.claude/commands/` has all 5 command files
- [ ] `6_Semblance/error.log` and `fix.log` initialized
- [ ] `CLAUDE.md` generated by `/init`
- [ ] `index.html` present at repo root
- [ ] First commit pushed to remote
